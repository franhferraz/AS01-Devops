   - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'  # ou a versão que você está usando

    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: npm test
